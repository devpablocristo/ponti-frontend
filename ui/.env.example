# API base URL (v1)
VITE_API_BASE_URL=/api/v1

# AI proxy URL (defaults to /api/v1/ai if not set)
VITE_AI_PROXY_URL=

# AI provider UI mode: legacy | axis (defaults to legacy)
VITE_AI_PROVIDER=legacy
